#!/usr/bin/env python
#
# I NEED AN ALGORITHM! :D
#

class Answer:
    """
    A class to handle answers.
    
    It needs to take a string and save it, generate a mask to hide all of the letters,
    randomly choose a character to unmask, show the masked clue, do this a few times,
    and be able to show the answer on command.
    """
    clueletters = []

    def __init__(self, string):
        self.original = string[:]
        self.masked = self.mask(self.original)

def getQuestion(fd,fLen):
    
if __name__ == "__main__":
    
